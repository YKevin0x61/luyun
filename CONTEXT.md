# CONTEXT

LuckIn 点餐数据系统的领域词汇表（ubiquitous language）。
只收录领域术语与其精确含义，不含实现细节。

## KDS（后厨控菜）

### 档口 (Station)
后厨按菜品品类划分的制作工位，如肠粉档、熟笼档、西饼档等。档口定义的唯一权威来源是后端
`config.py` 的 `KITCHEN_STATIONS`（前端经 `/api/stations` 获取）。菜品经 dish→station 映射归属到某个档口，未映射的落入「其他档口(qita)」兜底。

### 菜品档口目录 (Dish Catalog)
菜品名称 → 档口 的**唯一权威**解析与维护：查出所属档口、写入/删除显式映射、使后续解析看到新结果、把映射回填到订单、列出尚未映射的菜品。
采集入库、对账补录、管理后台「补充档口 / 未分类菜品」、数据质量「未映射」告警都走同一套语义。
不含「从历史订单猜最常档口」——那只是半成品规则等展示侧的兜底，不是权威映射。
底层经 DishStationsPort / OrdersPort，不直接碰 `table().conn`。
_Avoid_: 把订单历史频次推断当成档口目录的一部分。

### 领域端口 (Domain Ports)
`OrdersPort` / `DishStationsPort` / `ReportsPort` 是可替换的领域 seam（`db.orders` 等为独立 adapter，≠ DatabaseManager）。
服务层依赖 Protocol；Admin 通用 CRUD 仍走 TableView，不进 domain port。
_Avoid_: 把 port 当成 DatabaseManager 的 identity 别名，或在服务里再开 `table().conn` 旁路。

### 等待紧急度 (Wait Urgency)
按「等待时长」把待出餐工作量标为紧急 / 较急 / 普通。语义对齐（默认较急 15 分钟、紧急 20 分钟），但两端各自取阈值：
- 后端：读 `config.PRIORITY_LEVELS`（经 urgency policy）。
- KDS：读本屏本地告警参数 `warnMin` / `urgentMin`（经 `getTimeThresholdsMs`）。
两端不通过同步 API 互相覆盖。

### KDS 首页 (Hub)
进厨房控制台之前的本屏总览与入口面：呈现本屏职责档口集的忙闲与连接健康，并由此进入厨房控制台或系统设置等。
_Avoid_: 主页面（易与厨房控制台混淆）

### 厨房控制台 (Kitchen Console)
厨师在屏上进行控菜操作的工作面（待出餐工作、出餐、本屏告警与档口切换）。
出餐有两条互斥路径：卡上出餐、选桌出餐。
_Avoid_: 主页面；把 KDS 首页称作厨房控制台

### 菜卡 (Dish Card)
厨房控制台上同一菜名的一组待出餐份，是卡上出餐与选桌出餐的作用对象。可以是该菜全部待出餐，也可以是拆卡后的一截。
_Avoid_: 卡片（无限定）；把菜卡当成一张订单；小卡片（卡内订单行芯片）

### 卡上出餐 (Card Serve)
在厨房控制台菜卡上按份累加点选（从 0 起），经悬浮确认后出餐。选中份数按该卡内订单行的下单时间从早到晚分配，不在此路径指定桌号。
_Avoid_: 把点菜品卡当成新单已确认；把卡上点选当成选桌

### 选桌出餐 (Table-pick Serve)
在厨房控制台按订单行勾选后出餐：一行一份，同一桌多份为多行；在选桌面内确认。与进行中的卡上出餐选中互斥——进入选桌或以选桌出餐后，卡上选中作废。
_Avoid_: 详情（只读）；与卡上份数计数合并成一套选中

### 将出预览 (Serve Preview)
卡上出餐选中份数大于 0 时，只读标出 FIFO 将完成的订单行：对应小卡片实心绿底白字。不另起「将出」文案条。改出哪几桌只能走选桌出餐。
_Avoid_: 把将出预览做成可点选的选桌控件；不要用单独的「将出 xxx」横条。

### 拆卡 (Card Split)
把同一菜名的待出餐份分成多张菜卡。不同浪潮不共卡；同一浪潮内再按菜卡份数上限切。两路约束可单独开、可同时开。
_Avoid_: 入库按份拆分（那是订单行，不是菜卡）；跨菜名拆；后浪回填前浪的份数空洞

### 菜卡份数上限 (Dish Card Quantity Cap)
单张菜卡允许容纳的最大待出餐份数。0 表示不按份数拆。本屏本地，取值 0 或 1–99。
_Avoid_: 卡片份数；N

### 下单间隔 (Order Gap)
本屏分钟阈值：同一菜名按 FIFO 相邻两笔待出餐的下单时间之差达到或超过该值时，后一笔起新浪潮。0 表示不按间隔拆。取值 0 或 1–99。
_Avoid_: 拆卡间隔；时间拆分；卡内时间跨度；时钟桶；等待紧急度（告警用，不用来拆卡）

### 浪潮 (Order Wave)
同一菜名按下单时间 FIFO 排列、相邻间隔小于下单间隔的待出餐连续段。下单间隔为 0 时该菜只有一个浪潮。
_Avoid_: 时钟桶；把浪潮当成菜卡（一个浪潮可被份数上限切成多张菜卡）

## POS 采集

### 餐厅采集组合根 (Restaurant Scraper)
采集运行时的组合根：持有 PosSession + 桌台变化检测 + 外卖账单跟踪 + 状态存储，并拥有单轮编排（`run_cycle`）与状态 DTO（`get_status`）。
main 主循环与对账/登录探针只依赖本组合根公开面，不再经浅层 adapter facade。
_Avoid_: 再建一层只做 1:1 转发的 adapter。

### POS 会话 (POS Session)
与龙管家 POS 的浏览器会话：登录/恢复、营业时段门禁、忙桌与桌情拉取解析（经公开 fetch_table_orders）。不负责「桌台是否变化」的判定；底层 form POST / 会话失效重试交给 POS HTTP 客户端。

### POS HTTP 客户端 (POS HTTP Client)
共享的 POS 侧 HTTP：cookie/header、form POST、会话失效恢复回调、失败计数。由 POS 会话提供 page 与 recover；外卖已结与桌情明细共用。

### 桌台变化检测 (Table Change Detector)
对比上一轮桌台金额与菜品快照，只产出增量订单（含 flow_id 分配）；快照读写交给采集状态存储。

### 外卖账单跟踪 (Delivery Bill Tracker)
已结账单/外卖单的拉取、去重采集，以及「列表消失即取消」的累计判定与自愈。

### 采集状态存储 (Scraper State Store)
桌台快照与外卖去重/缺席计数的持久化（按营业日 06:00 切换清空），供桌台变化检测与外卖账单跟踪共用。

### 入库订单行构建 (Order Line Builder)
把「已解析的原始菜行」拆成可入库的订单行：按份拆分、分配 business_flow_id、解析档口、分类、统一金额与时间字段。
堂食桌情、外卖已结、对账补录共用同一套形状；各自仍负责解析自己的 POS JSON。
桌台变化检测里的增量加菜/退菜 flow_id 分配不在此列。
_Avoid_: 在采集各入口各自拼入库 dict。

### 屏幕职责档口集 (Watched Stations)
单台 KDS 设备（一块屏）所负责、需要接收提醒的档口集合。
- 主力档口屏：职责档口集为单个档口（一屏一档），厨师只关心本档口。
- 总控/小档口共享屏：职责档口集为多个或全部档口（一屏多档）。
是**按设备本地配置**的属性，不随后端全局设置变化。新单等告警只对职责档口集内的档口触发，
并决定厨房页可见/可切换的档口 Tab（单档口锁定全屏、隐藏 Tab 栏；多档口只显示这几个 Tab）。

### 厨房订单会话 (Kitchen Order Session)
厨房页的一次「拉取 → 分发」边界：拉取当天订单后同步给新单告警与外卖取消引擎，并暴露本屏职责档口集与本地等待紧急度阈值驱动的档口统计/超时装饰。
页面不直接接线两个告警 composable；出餐、打印、断连告警仍留在页面。
_Avoid_: 在页面里对同一批订单分别手写两套 sync。

### Nudge→Pull 合并 (Nudge Pull Coalesce)
客户端收到同订阅上的连续 nudge 后，在短窗（默认 300ms，对齐服务端 dashboard debounce）内合并为一次 HTTP 重拉。
KDS 与 Admin 共用该策略；60s reconcile / 断线 fallback 立即重拉，不进合并窗口。
_Avoid_: 在服务端再叠一层 per-connection debounce 替代客户端合并。

### 新单事件 (New-order Event)
本屏职责档口集内「待出餐工作量净增」的事件，是新单提醒（声音/振动/视觉）的触发条件。
满足以下任一即构成新单事件：
- 出现一条上一轮快照中不存在的待出餐菜品行（新的 business_flow_id）。
- 已有菜品行的待出餐份数增加，或同桌新增了另一道菜（加菜/加量）。
- 曾退菜/取消的菜品重新变回待出餐状态（退后重下）。
非职责档口集内的变化不构成新单事件。

### 外卖取消事件 (Delivery-cancel Event)
本屏职责档口集内、且 `source` 为外卖的订单，其出餐状态由非「已取消」变为「已取消」的事件。
用于厨房屏外卖取消横条与强提醒。首次进入页面只建立基线、不对历史已取消单告警；
非职责档口或非外卖来源的取消不构成此事件。

### 新单已确认 (Acknowledged)
厨师通过显式动作，将当前全部未确认新单一次性标记为已知晓。
确认途径：点「已知晓」按钮；黄闪期间点屏幕四周热区，或点菜品区空白。
点菜品卡加份、出餐、选桌出餐、顶栏、档口切换、排序、将出预览、出餐条均不算确认。
确认会停止黄闪与声音兜底重提。确认之后若再来新单，重新进入未确认状态。
「已确认」表示厨师已**知晓**新单，不代表已出餐（已完成制作）。
_Avoid_: 把出餐或点菜品卡当成已知晓

### 屏幕状态边框 (Ambient Status Border)
KDS 屏幕四周的粗边框，作为可远距离感知的红绿灯式档口状态指示，反映本屏职责档口集的整体状态：
- 🟢 绿：无待出餐单（全部已完成制作）。
- 🟡 黄（闪烁）：存在未确认的新单，同时触发提示音及兜底重提。
- 🔴 红：已确认（已点「已知晓」，停闪静音），但仍有待出餐单未完成制作。
超时（等待紧急度已到紧急）不占用边框状态，避免压过黄闪；超时由菜品卡样式与超时提示音表达。
_Avoid_: 把超时做成边框第四态；未确认新单时用超时脉冲盖掉黄闪

告警强度按档口忙闲分级（同一新单事件的响应因触发时状态而不同）：
- 空闲→来活（触发前本档口无待出餐单，🟢 绿）：转 🟡 黄闪 + 升级重提 + 必须完成新单已确认才转 🔴 红。对应厨师可能没盯屏的高危时刻，需强提醒。
- 忙碌中（触发前本档口已有待出餐单，🔴 红）：仅响一声轻提醒，保持 🔴 红，不黄闪、不要求再确认。

### 新单角标 (New Badge)
待出餐菜卡上标示「本条为新单」的角标（含高亮边框）。清除时机按忙闲：
- 空闲→来活（黄闪）场景：新单已确认后一次性清除全部新单角标。
- 忙碌中（红）场景：角标显示一段时间后自动消退（默认厨师已余光看到）。

## 出餐状态 (Dish Status)
菜品在后厨的生命周期状态：待出餐 → 已制作待上菜 → 已上菜；另有终态「退菜/已取消」。
「出餐」指厨师把某道菜标记为已制作完成（待出餐 → 已制作待上菜）的动作。

## 部署与更新

### 发行版 (Release)
一次可安装、可更新的系统交付物，以 GitHub Release（附带版本 tag）为唯一宣告源。
每个正式 Release 附带**发行包**；运行实例不跟随 branch 漂，「有没有新版本」只对照远端正式 Release。
_Avoid_: 把任意 commit / 未打 tag 的分支 tip 当成发行版；要求生产机构建前端。

### 发行包 (Release Bundle)
某一正式发行版的单一归档交付物：应用代码树、已构建的 Admin SPA 与 KDS、以及版本清单与完整性校验材料。
不包含可写业务数据、凭据、密钥，也不包含 Playwright 浏览器。装机与升级使用同一种发行包。
_Avoid_: 以 git clone/checkout 充当店内程序交付；把 `data/` 打进发行包；以 Docker 镜像作为本系统的店内交付真相（容器可以只是运行外壳）。

### 版本清单 (Release Manifest)
装在运行实例上的已装发行版身份记录（随发行包落入部署目录）。版本检测以本机版本清单为准，并与远端正式 Release 对照；对不上则视为异常/非精确状态。
_Avoid_: 以部署目录 git tag、或以单独的 `APP_VERSION` 字段作为「已装发行版」的权威来源（`APP_VERSION` 仅作展示对齐，发版时与 tag 同步）。

### 运行实例 (Runtime Instance)
单机上正在运行的一套本系统部署（单实例、单 uvicorn worker）。版本检测与应用更新的作用对象是运行实例，不是开发机。
仅通过更新环境自检的运行实例才允许应用更新；开发机、脏工作区等非运行实例在界面上不提供应用更新。

### 版本检测 (Version Check)
对比本机已装发行版（版本清单）与远端可用正式发行版，并给出能否应用更新的自检结论；不改变运行中的代码。

### 更新环境自检 (Update Preflight)
版本检测时对「下载发行包、校验、切换、重启」所需条件的只读检查。不满足则禁止应用更新（红灯）；脏部署目录默认禁止，仅在管理员明确确认丢弃本地改动后方可继续。
_Avoid_: 点了应用更新才发现缺重启能力或凭据。

### 应用更新 (Apply Update)
把运行实例切到选定正式发行版：由已登录的管理后台发起；实际执行交给独立的更新作业，不在 Web 进程内原地改代码。
回滚即对更旧正式发行版再执行一次应用更新（再下载该版发行包）。
_Avoid_: 在运行实例上再跑前端构建；在不备份、不确认目标发行版的情况下静默覆盖；让 FastAPI 进程自己替换自身后自杀；以 `docker pull` 镜像作为本系统的标准升级路径。

### 更新作业 (Update Job)
一次应用更新的旁路执行单元（主机上由 systemd oneshot 拉起，或在 Docker 运行外壳内以后台进程拉起）：强制备份成功后，下载并校验目标发行包，旁路解压并原子切换应用目录（保留上一版目录以便失败回退），按需同步 Python 依赖，再重启主服务，并留下可查询的进度与结果。
中途失败时回到更新前的已装发行版并尽量拉起主服务；管理后台只负责发起、展示、自检结论与选择目标发行版。
_Avoid_: 更新作业在 Web 请求线程里做完下载与切换；失败后停在半截目录无人理会；把更新作业做成常驻多实例工作者。

### 引导安装 (Bootstrap Install)
新机器从零装到「应用进程可启动」：从公开 GitHub Release 匿名取得目标发行包并解压、安装 Python/Playwright 依赖、启主服务（及更新作业的拉起方式）。不要求生产机安装 Node，也不要求 Deploy Key / git clone / Releases PAT（PAT 仅在 API 限流时可选）。
可用 systemd 宿主机路径，或 Docker / Compose 进程外壳（`deploy/docker-compose.yml`：挂载直播树的**父目录** + `docker.sock`；升级仍走发行包 / Admin「系统更新」）。
不含反代/TLS/域名与 POS 凭据填写——那些仍是人工一步。
_Avoid_: 引导安装以私有仓 clone 或强制 PAT 为必经步骤；把 Docker 镜像 pull 当成店内交付真相。

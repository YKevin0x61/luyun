fastapi==0.104.1
uvicorn[standard]==0.24.0
aiosqlite>=0.19.0
pandas>=2.0.0
openpyxl>=3.1.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<5
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.25.2
# 钉死：playwright 的 lib 与浏览器 build 强绑定（1.63.0 ↔ chromium/headless-shell 1243）。
# 放开上界会让「系统更新」的 pip sync 单方面升级 lib，而浏览器二进制不会跟着换，
# 直接导致 scraper 报 Executable doesn't exist。升级 lib 时必须同步执行
# `.venv/bin/python -m playwright install chromium`
# （更新作业与 Docker entrypoint 已内置该步骤，见 services/playwright_env.py）。
playwright==1.63.0
beautifulsoup4==4.12.2
lxml==4.9.3
requests==2.31.0
aiohttp==3.9.1
psutil==5.9.6
markdown>=3.5,<4
python-docx>=1.1,<2
pillow>=10.0,<12
pytest>=7,<9
